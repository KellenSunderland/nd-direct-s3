{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Debug Build",
            "type": "shell",
            "command": "mkdir -p ${workspaceFolder}/build/ && cd ${workspaceFolder}/build/ && cmake .. -DCMAKE_EXPORT_COMPILE_COMMANDS=ON -DCMAKE_BUILD_TYPE=DEBUG && make -j $(nproc)"
        }
    ]
}